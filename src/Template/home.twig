{% extends 'layout.twig' %}

{% block content %}
    <div class="row">
    {% for index, product in products %}
        <div class="col-xl-3 col-lg-3 col-md-4 col-sm-6">
            {% include 'Element/product_list.twig' with {product:product} %}
        </div>
    {% endfor %}
    {% if(index +1)%3 == 0 %}
        </div>
        <div class="row">
    {% endif %}
    </div>
{% endblock %}